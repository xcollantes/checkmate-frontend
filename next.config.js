/**
 * NextJS configuration for web app.
 */


/**
 * Listing of domains allows for calls to external source.
 */
module.exports = {
    images: {
        domains: [
            "storage.cloud.google.com",
            "storage.googleapis.com",
            "firebasestorage.googleapis.com"
        ]
    }
}